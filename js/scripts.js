var add = function(number1, number2){
	return number1 + number2;
};

var firstAdd = parseInt(prompt("Enter a number:"));
var secondAdd = parseInt(prompt("Enter another number:"));

alert(add(firstAdd, secondAdd));
